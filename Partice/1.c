#include <stdio.h>
int main()
{
    int marks;
    char first_name;
    float pi;
    scanf("%d %c %f", &marks, &first_name, &pi);
    printf("a num %d, a char %c, a float value %f", marks, first_name, pi);
}